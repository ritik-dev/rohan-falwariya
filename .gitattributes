# Normalise to LF in the repo; Git checks out CRLF on Windows as it likes.
* text=auto eol=lf

# Binaries — never touch these.
*.woff2 binary
*.jpg   binary
*.png   binary
*.pdf   binary
*.zip   binary
