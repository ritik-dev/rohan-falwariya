See **AGENTS.md** for how this project is built and the rules for changing it,
and **chats.md** for the full session history and open items.

Quick version: edit `src/page.html`, run `node build.mjs`, never hand-edit
`index.html` or `dist/`.
